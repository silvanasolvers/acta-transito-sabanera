{
  "name": "acta-sabanera-landing",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "mkdir -p dist && cp index.html dist/index.html",
    "start": "npx serve dist -l 3000"
  }
}
